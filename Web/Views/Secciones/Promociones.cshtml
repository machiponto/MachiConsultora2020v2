@model IEnumerable<BI.ClsPromocion>

@{
    ViewBag.Title = "Promociones";
}

<h2>Promociones de Machi Consultora</h2>

@if (Model != null)
{
    foreach (var _promo in Model)
    {
    <div class="bs-docs-section">

        <div class="row">
            <div class="col-lg-8">

                <div class="well">

                    <div class="bs-example">
                        <table class="table table-striped table-bordered table-hover">

                            <tbody>
                                <tr>
                                    <td>
                                        @Session["Nombre"]:
                                    </td>
                                    <td>

                                        @*@Html.DisplayFor(modelItem => _promo.nombre)*@
                                    </td>
                                </tr>
                                <tr>
                                    <td>
                                        @Session["Descripcion"]:
                                    </td>
                                    <td>
                                      @*  @Html.DisplayFor(modelItem => _promo.descripcion)*@
                                    </td>
                                </tr>
                        
                           
                                <tr>
                                </tr>
                            </tbody>
                        </table>

                        <div class="row">

                           @* @Html.Partial("~/Views/Secciones/_ListarProductosEnPromocion.cshtml", _promo.listadoProductos)*@

                        </div>

                        <form action="/Carrito/agregarPromo" method="post">
                            @*<input type="hidden" name="idPromo" value="@_promo.idPromocion" />*@
                            <input type="hidden" name="pSession" value="@Session.SessionID" />
                            <input type="submit" value="Agregar..." class="btn btn-primary" />
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    }
}
